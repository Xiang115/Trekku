# Base URL for the Trekku FastAPI backend.
# Leave EMPTY in development to use the Vite dev proxy (vite.config.ts),
# which forwards /agent, /ratings and /health to http://localhost:8000.
# Set this to an absolute URL (e.g. https://api.trekku.example) in production.
VITE_API_BASE_URL=
